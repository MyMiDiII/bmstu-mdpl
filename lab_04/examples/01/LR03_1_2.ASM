; идентификатор доступный из других модулей
PUBLIC output_X
; идентификатор X типа byte определен в другом модуле
EXTRN X: byte

; сегмент, расположенный по адресу видео пам€ти
DS2 SEGMENT AT 0b800h
	CA LABEL byte
	; положение символа 
	; размер строки на еЄ номер (+1 из-за перехода 
	;на новую строку после выполнени€) размер символа
	; + номер столбца
	; последующий код за обозначенным адресом
	ORG 80 * 2 * 2 + 2 * 2
	SYMB LABEL word
DS2 ENDS

; сегмент с выравниванием по адресу кратным 16,
; с соединением всех сегментов с одинаковыми именами
CSEG SEGMENT PARA PUBLIC 'CODE'
	assume CS:CSEG, ES:DS2

; процедура c с переходом в пределах того же семента
output_X proc near
	; запись адреса сегмента данных DS2 в
	; сегментный регистр ES
	mov ax, DS2
	mov es, ax
	; запись атрибутов символа 1010 (€ркий зеленый)
	mov ah, 10
	; запись символа под именем X
	; в данном случае R (из другого модул€)
	mov al, X
	; запись полной информации о символе в видеопам€ть
	mov symb, ax
	; возврат из подпрограммы
	ret
output_X endp
CSEG ENDS
END